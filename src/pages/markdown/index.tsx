import React from 'react'
import ReactMarkdown from 'react-markdown';
// import Md from './DayDayUp.md';

const Markdown = () => {
    return (
        <div>
           <ReactMarkdown sourcePos source="./DayDayUp.md"></ReactMarkdown> 
        </div>
    )
}

export default Markdown
